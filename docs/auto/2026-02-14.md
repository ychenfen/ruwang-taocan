# 自动/手工推进记录 - 2026-02-14

## 本次目标
- 按 `docs/PLAN.md` 推进 M3（Auth/RBAC）并保持测试可运行。

## 变更摘要
- backend:
  - 新增认证接口：`POST /auth/login`、`GET /auth/me`
  - 新增管理员示例 CRUD：`/admin/agent-levels`（需要 ADMIN JWT）
  - 新增密码哈希：scrypt + salt（含单测）
  - 新增 `seed` 脚本（创建开发环境 admin 账号）
  - 增加 backend vitest 与 typecheck
- docs:
  - 更新 `docs/PROGRESS.md`：M3 的 `/auth/*` 已完成

## 运行的命令与结果
- `npm install`：成功
- `npm test`：成功（commission-engine + backend tests）
- `npm run typecheck`：成功

## Blockers
- Docker daemon 当前不可用（提示无法连接到 docker.sock），因此未能验证：
  - `docker compose up -d` + `npm run migrate` + `npm run seed` 的完整链路

## 下一步建议
1. 启动 Docker Desktop / OrbStack 后验证 DB 全流程（compose + migrate + seed）。
2. 实现 RBAC scope（AGENT 只能访问 self + 一级/二级下级）并补齐代理端查询接口。
3. 开始实现结算写库（DRAFT 生成、按代理重算范围、POSTED 调整单）。

