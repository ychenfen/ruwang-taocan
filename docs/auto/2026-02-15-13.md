# 自动/手工推进记录 - 2026-02-15 (13)

## 本次目标
- 完成性能优化里程碑：关键索引 + 慢查询基线。

## 变更摘要
- 数据库索引迁移
  - 新增迁移：`backend/migrations/006_perf_indexes.sql`
  - 新增索引覆盖：
    - 时态归属/关系查询：`card_assignments`、`agent_relations`、`team_memberships`
    - 结算运行与行项目：`settlement_runs(created_at)`、`settlement_items`（run/kind/beneficiary/created/adjustment）

- 慢查询基线脚本
  - 新增：`backend/scripts/slow-query-baseline.ts`
  - 新增命令：
    - `backend/package.json` -> `perf:baseline`
    - 根 `package.json` -> `perf:baseline`
  - 输出产物：`docs/perf/slow-query-baseline.md`

- Runbook
  - 新增：`docs/runbooks/performance-baseline.md`
  - 包含执行步骤、环境变量、判读口径与回归要求。

- 进度更新
  - `docs/PROGRESS.md`：`性能优化：关键索引 + 慢查询基准` 标记完成。

## 验证结果
- `npm run migrate`：成功（应用 `006_perf_indexes.sql`）
- `npm run perf:baseline`：成功（已写入基线文档）
- `npm run typecheck`：成功
- `npm test`：成功

## 基线结果（pglite，本地）
- report_agent_summary: 1.377 ms
- settlement_cards_scan: 0.784 ms
- adjust_base_items_scope: 0.097 ms
- settlement_relations_l1: 0.052 ms

## 结论
- 关键查询已具备更明确的索引命中路径，且有可重复执行的基线产物用于后续回归对比。
