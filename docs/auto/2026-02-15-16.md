# 自动/手工推进记录 - 2026-02-15 (16)

## 本次目标
- 继续优化仪表盘查询效率：补齐 Agent 端聚合统计接口，减少首页多请求。

## 变更摘要
- 新增 Agent 统计接口
  - `GET /agent/stats`
  - 返回：
    - `me`（姓名/星级/团队）
    - `myOnNetCardCount`
    - `downlineLevel1Count`
    - `downlineLevel2Count`
    - `teamMemberCount`
    - `teamOnNetCardCount`
  - 文件：`backend/src/routes/agent/index.ts`

- 新增 Agent 统计测试
  - 文件：`backend/src/routes/agent/stats.test.ts`
  - 覆盖：代理首页关键计数（本人在网、一级/二级下级、团队成员、团队在网）

- 权限回归补充
  - 将 `/agent/stats` 纳入 RBAC 回归用例
  - 文件：`backend/src/routes/rbac.test.ts`

- Agent 首页改造
  - 从多接口拉取（`/agent/me` + `/agent/cards` + `/agent/downlines` + `/agent/team-members`）
  - 改为主用单接口 `/agent/stats`
  - 文件：`agent-web/app/(app)/dashboard/page.tsx`

## 验证结果
- `npm -w backend test`：成功（14 文件、22 用例通过）
- `npm run typecheck`：成功
- `npm -w admin-web run build`：成功
- `npm -w agent-web run build`：成功

## 结论
- Admin/Agent 两端总览均已切换到聚合统计口径，首页请求更轻、展示一致性更好。
