# 自动/手工推进记录 - 2026-02-15 (14)

## 本次目标
- 完成 M8 未收尾项：关键接口权限回归测试。

## 变更摘要
- 新增权限回归测试文件：
  - `backend/src/routes/rbac.test.ts`
- 覆盖场景：
  - AGENT 访问关键 `/admin/*` 接口应返回 `403 FORBIDDEN`
  - ADMIN 访问 `/agent/*` 接口应返回 `403 FORBIDDEN`
  - 未登录访问 `/admin/*` 与 `/agent/*` 应返回 `401 UNAUTHORIZED`
  - `/agent/downlines/cards` 越权传入非下级 `agentId` 应返回 `403 FORBIDDEN`
  - `AGENT` 账号缺失代理档案时应返回 `403 NO_AGENT_PROFILE`

- 文档更新：
  - `docs/PROGRESS.md`：M8 权限回归项标记完成。

## 验证结果
- `npm run typecheck`：成功
- `npm test`：成功（backend 12 文件、19 用例全部通过）

## 结论
- 权限边界已形成可回归保护网，后续新增接口可按同模式继续补用例。
