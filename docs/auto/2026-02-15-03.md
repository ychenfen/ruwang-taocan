# 自动/手工推进记录 - 2026-02-15 (03)

## 本次目标
- 把“佣金引擎”接入数据库：能够生成结算草稿（DRAFT）并写入行项目。
- 支持按代理重算（只刷新该代理作为收益人的行项目）。

## 变更摘要
- backend:
  - 新增结算计算模块：`backend/src/settlement/compute.ts`（从 DB 取卡/归属/状态/上下级并调用共享引擎计算）
  - 新增结算接口（ADMIN）：
    - `POST /admin/settlements/recalculate`（支持 `agentId`）
    - `GET /admin/settlements/runs`
    - `GET /admin/settlements/runs/:id/items`
  - 修复月份 CHECK 约束兼容性：新增迁移 `backend/migrations/002_fix_month_checks.sql`
  - 调整网卡初始归属起始时间：初始 `card_assignments.start_at` 对齐激活日期（便于回算历史月）
  - 新增接口测试：`backend/src/routes/admin/settlements.test.ts`
- docs:
  - `docs/PROGRESS.md`：勾选“按代理重算”

## 运行的命令与结果
- `npm run migrate`：成功（应用 `002_fix_month_checks.sql`）
- `npm run typecheck`：成功
- `npm test`：成功

## 下一步
1. 自动跑批：每月 5 号自动结算上月（加锁 + 幂等 + 运行记录）。
2. 结算状态流转：DRAFT -> APPROVED -> POSTED；POSTED 后走调整单。
3. 报表导出：按月导出卡明细 + 代理/团队汇总（CSV/Excel）。

