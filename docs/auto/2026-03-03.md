# 2026-03-03 Run Report

## What changed
- Investigated SSH deployment blocker for server `114.67.88.220`.
- Confirmed timeout occurs during SSH banner exchange (pre-auth), not during key auth.
- Prepared server-console recovery command set for user to restore SSH daemon availability.

## Commands
- `nc -vz -w 5 114.67.88.220 22`
- `ssh -vvv -i /Users/yuchenxu/Downloads/bot.pem -o ConnectTimeout=8 -o StrictHostKeyChecking=no root@114.67.88.220 'date'`
- Python socket probe (connect to `114.67.88.220:22`, wait for banner bytes)

## Results
- TCP port 22 reachable.
- SSH command still fails with `Connection timed out during banner exchange`.
- Direct socket connect succeeds but no SSH banner is returned within timeout window.
- Indicates server-side SSH service path issue (daemon/firewall/fail2ban/throttle), not key-file content.

## Next step
- User runs provided server-console commands to restart/check SSH service and unban/open SSH path.
- After SSH recovers, deploy pending fixes for issue #1/#3 and restored UI controls #4/#5.

## Blockers
- Cannot deploy while SSH handshake remains stuck at banner stage.

## Follow-up
- User provided root account password for fallback login.
- Tried password-based SSH (`PreferredAuthentications=password`, `PubkeyAuthentication=no`) and interactive password login.
- Result unchanged: still timeout/close around banner stage before authentication.
- Conclusion unchanged: this is server-side SSH service path issue, not key or password credential issue.

## Follow-up 2
- User ran server-console checks and confirmed:
  - sshd service active and listening on `*:22`
  - UFW already allows 22/tcp
  - fail2ban not loaded
  - sshd log output currently empty for this issue window
- Local re-test from automation environment still fails at banner stage.
- Added next recovery step: explicitly allow automation source IP (`62.192.175.177/32`) in UFW and retry.
