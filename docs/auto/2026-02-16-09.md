# 自动/手工推进记录 - 2026-02-16 (09)

## 本次目标
- 继续完善“可运营”能力：新增导出频次预警，并补齐审计导出全链路留痕。

## 变更摘要
- 运营预警增强（后端）
  - 文件：`backend/src/routes/admin/stats.ts`
  - `GET /admin/stats/alerts` 新增导出频次异常预警：`EXPORT_VOLUME_SPIKE`
    - 新增参数：
      - `exportLookbackHours`（默认 24）
      - `exportCountThreshold`（默认 20）
    - 统计导出动作：
      - `REPORT_EXPORT_SETTLEMENT_ITEMS`
      - `LEDGER_EXPORT_ENTRIES`
      - `AUDIT_EXPORT_LOGS`
    - 告警输出包含：总导出量、窗口、阈值、涉及账号数、最高频账号、最高频动作、时间范围。

- 审计导出留痕补齐（后端）
  - 文件：`backend/src/routes/admin/audit-logs.ts`
  - `GET /admin/audit-logs.csv` / `GET /admin/audit-logs.xlsx` 新增审计写入：
    - `action=AUDIT_EXPORT_LOGS`
    - `entityType=audit_logs`
    - `meta` 包含 `format/filters/rowCount`
  - `GET /admin/audit-logs/export-summary` 的 action 白名单扩展至 `AUDIT_EXPORT_LOGS`。

- Admin 前端适配
  - 文件：`admin-web/app/(app)/dashboard/page.tsx`
    - 预警文案更新，包含“导出频次”来源。
  - 文件：`admin-web/app/(app)/audit-logs/page.tsx`
    - 导出动作白名单扩展 `AUDIT_EXPORT_LOGS`
    - 快速筛选新增“审计导出”。

- 回归测试增强
  - 文件：`backend/src/routes/admin/stats.test.ts`
    - 增加 `EXPORT_VOLUME_SPIKE` 触发断言。
  - 文件：`backend/src/routes/admin/audit-logs.test.ts`
    - 增加审计导出后产生 `AUDIT_EXPORT_LOGS` 断言。

- 工程稳定性修复
  - 发现 `next build` 偶发报错：`ERR_INVALID_PACKAGE_CONFIG` / `MODULE_NOT_FOUND`（`next` 依赖异常状态）。
  - 处理：执行 `npm ci --force` 进行干净重装。
  - 文件：`.node-version`
    - 固定 Node 版本 `22.17.0`，降低本地环境差异导致的构建不稳定。
  - 文件：`package.json`
    - `typecheck` 调整为后端口径：`npm -w backend run typecheck`（前端类型检查由 `next build` 覆盖）。
  - 文件：`README.md`
    - 新增 Node 版本建议（22.x）与验证口径说明。

## 验证结果
- `npm -w backend test -- src/routes/admin/stats.test.ts src/routes/admin/audit-logs.test.ts` 通过
- `npm test` 通过（佣金引擎 + 后端全量）
- `npm -w backend run typecheck` 通过
- `npm -w admin-web run build` 通过
- `npm -w agent-web run build` 通过
- `fnm exec --using v22.17.0 -- npm run verify` 通过

## 结论
- 管理端“导出行为”从可追溯进一步升级为可预警。
- 审计、报表、分录三类导出已形成统一审计口径。
- 依赖层稳定性已修复，当前可继续推进下一批功能。
