# 自动/手工推进记录 - 2026-02-16 (01)

## 本次目标
- 继续按计划完善，补齐“审核入账生成分录”的完整闭环（后端 + 前端 + 测试）。

## 变更摘要
- 数据库迁移
  - 新增 `backend/migrations/008_ledger_entries.sql`
  - 新增分录主表/明细表：
    - `ledger_entries`
    - `ledger_entry_lines`
  - 增加唯一键和查询索引（source 唯一、run/month、entry lines 等）。

- 后端：分录写入能力
  - 新增 `backend/src/ledger/entries.ts`
    - `createLedgerEntryForPostedRunIfMissing`
    - `createLedgerEntryForAdjustments`
  - 结算 POST 逻辑改造（事务内）：
    - `APPROVED -> POSTED` 后自动写 `SETTLEMENT_POST` 分录
    - 文件：`backend/src/routes/admin/settlements.ts`
  - 调整逻辑改造：
    - 调整批次增加 `adjustmentBatchId`
    - 调整成功后自动写 `SETTLEMENT_ADJUST` 分录
    - 返回新增字段：`adjustmentBatchId`、`ledgerEntryId`、`insertedItemIds`
    - 文件：`backend/src/settlement/adjust.ts`

- 后端：Ledger 查询 API
  - 新增路由文件：`backend/src/routes/admin/ledger.ts`
    - `GET /admin/ledger/entries`
    - `GET /admin/ledger/entries/:id/lines`
  - 注册到 admin 路由：`backend/src/routes/admin/index.ts`

- 测试
  - 新增 `backend/src/routes/admin/ledger.test.ts`
    - 覆盖：POST 分录生成 + 调整分录生成 + 明细查询
  - RBAC 回归补充 `/admin/ledger/entries` 禁止 AGENT 访问
    - 文件：`backend/src/routes/rbac.test.ts`

- 前端（Admin）
  - 新增页面：`admin-web/app/(app)/ledger/page.tsx`
    - 支持按月份/来源/结算单/收益人筛选分录
    - 支持查看分录行明细与分页
  - 侧边栏新增入口：`admin-web/components/Sidebar.tsx`

- Node 24 稳定性补强
  - shared 引擎测试脚本加入 `NODE_OPTIONS=--no-experimental-require-module`
  - 文件：`shared/commission-engine/package.json`

- 文档
  - 更新：`docs/PROGRESS.md`
  - 更新：`docs/data-model.md`

## 验证结果
- 迁移：`npm run migrate` 成功（应用 `007_agent_level_history.sql`、`008_ledger_entries.sql`）
- 后端测试：`npm -w backend test` 成功（17 files / 29 tests）
- shared 引擎测试：`npm -w shared/commission-engine test` 成功（1 file / 6 tests）
- 后端类型检查：`npm -w backend run typecheck` 成功
- Admin 构建：`npm -w admin-web run build` 成功（路由包含 `/ledger`）
- Agent 构建：`npm -w agent-web run build` 成功
- 接口冒烟：`GET /admin/ledger/entries` 返回 200

## 运行状态
- backend: `http://127.0.0.1:3000`（在线）
- admin-web: `http://127.0.0.1:3001`（在线）
- agent-web: `http://127.0.0.1:3002`（在线）
- 默认管理员：`admin / admin123456`

## 结论
- “入账分录”从需求描述已推进为可运行能力：POST/调整自动落分录、可查询、可在管理端查看明细，且通过回归测试验证。
