# 自动/手工推进记录 - 2026-02-15 (04)

## 本次目标
- 把“每月 5 号自动结算上月”的自动执行落到后端（本地常驻运行即可触发）。

## 变更摘要
- backend:
  - 抽出结算重算函数：`backend/src/settlement/recalculate.ts`（Admin 接口与定时任务复用）
  - 新增定时任务：
    - `backend/src/jobs/monthlySettlement.ts`：每月 5 号 00:10 自动结算上月（生成/刷新 DRAFT）
    - `backend/src/jobs/index.ts`：启动 jobs
  - `backend/src/server.ts`：启动服务时自动 `startJobs(app)`
- docs:
  - `README.md`：补充定时任务说明

## 运行的命令与结果
- `npm run typecheck`：成功
- `npm test`：成功

## 说明
- 当前按“单实例”设计可直接运行；若未来要多实例，需要加 DB 锁（advisory lock 或锁表）避免重复跑批。

