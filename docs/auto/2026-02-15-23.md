# 自动/手工推进记录 - 2026-02-15 (23)

## 本次目标
- 继续按计划完善，并把当前代码库修到“可稳定回归测试 + 可本地构建运行”。

## 变更摘要
- 后端结算口径修复（历史星级）
  - 结算计算改为：按结算月末取 `agent_level_histories`；若该月末无记录，则回退到该代理最早历史记录，再回退 `current_level_id`。
  - 文件：`backend/src/settlement/compute.ts`

- 报表口径同步修复（与结算一致）
  - `settlement-items.csv`、`settlement-items-preview`、`settlement-summary/agents` 的星级筛选/展示统一改为上述口径。
  - 文件：`backend/src/routes/admin/reports.ts`

- 回归测试稳定性增强
  - `vitest` 配置增加串行文件执行与超时配置（避免并发初始化造成 hook 超时）。
  - 文件：`backend/vitest.config.ts`

- Node 24 兼容修复
  - 后端测试脚本加入 `NODE_OPTIONS=--no-experimental-require-module`，规避 `vitest/vite/rollup` 启动兼容问题。
  - 文件：`backend/package.json`

- 新增历史星级回归用例修正
  - 回归用例改为显式回填历史生效时间（上下级关系/初始星级起始时间），避免被“关系生效晚于结算月”干扰。
  - 文件：`backend/src/routes/admin/settlements.test.ts`

- 文档
  - 更新进度追踪：`docs/PROGRESS.md`

## 验证结果
- `npm test`：成功
  - `shared/commission-engine`: 1 files / 6 tests 通过
  - `backend`: 16 files / 28 tests 通过
- `npm run typecheck`：成功（backend/admin-web/agent-web）
- `npm -w admin-web run build`：成功
- `npm -w agent-web run build`：成功

## 运行状态
- 后端：`http://127.0.0.1:3000`（在线）
- 管理端：`http://127.0.0.1:3001`（在线）
- 代理端：`http://127.0.0.1:3002`（在线）

## 结论
- 当前不是停留在 MVP：后端规则、历史复算、报表口径、回归测试与前端构建链路均已补齐并验证通过，可继续推进下一轮“运营增强/数据治理”项。
