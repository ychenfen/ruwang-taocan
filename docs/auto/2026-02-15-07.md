# 自动/手工推进记录 - 2026-02-15 (07)

## 本次目标
- 按“机器完善”要求补齐前端工作区与页面能力，不止 MVP。
- 保证本地无 Docker 可直接跑通（安装、测试、类型检查、构建、启动）。

## 变更摘要
- Workspace 与脚本
  - 新增 `agent-web/`（Next.js App Router）。
  - 根 `package.json`：
    - 保留 `admin-web` + `agent-web` workspaces。
    - `dev:backend` 增加开发兜底环境：`JWT_SECRET` 与 `TZ=Asia/Shanghai`。
    - `migrate/seed` 增加 `TZ` 兜底。
- 管理端（`admin-web/`）
  - 新增网卡页面：
    - `app/(app)/cards/page.tsx`（列表、过滤、创建）
    - `app/(app)/cards/[cardId]/page.tsx`（编辑基础信息、转移归属、状态事件）
  - 补齐模块规范：`admin-web/AGENTS.md`。
  - 修复若干 TS 严格模式问题（status 表单字面量类型、SWR 参数类型）。
  - Next 配置与脚本调整：
    - `next build/dev` 改为 webpack 模式，规避中文路径 Turbopack 崩溃。
    - `next.config.mjs` 增加 `outputFileTracingRoot`。
- 代理端（`agent-web/`）
  - 新增完整门户：
    - 登录：`/login`
    - 总览：`/dashboard`
    - 我的网卡：`/cards`
    - 我的团队：`/team`
    - 我的同事（一级/二级 + 差价 + 脱敏卡）：`/downlines`
    - 公告：`/announcements`
  - 共用能力：`lib/api.ts`、`lib/auth.ts`、`AuthGate`、`Sidebar`、全局样式。
  - 新增模块规范：`agent-web/AGENTS.md`。
  - Next 配置与脚本同样切换 webpack，并设置 `outputFileTracingRoot`。
- 后端
  - `backend/src/server.ts`：启动时设置 `process.env.TZ = app.config.TZ`，确保结算定时任务与月度口径一致（Asia/Shanghai）。

## 验证记录
- 依赖安装：
  - `npm install`：成功
- 测试：
  - `npm test`：成功（commission-engine + backend 全部通过）
- 类型检查：
  - `npm run typecheck`：成功（backend + admin-web + agent-web）
- 前端构建：
  - `npm -w admin-web run build`：成功（webpack）
  - `npm -w agent-web run build`：成功（webpack）
- 本地运行（后端）：
  - `npm run dev:backend`：成功启动
  - `GET /health`：`{"ok":true}`
  - `GET /health/db`：`{"ok":true}`

## 结论
- 当前仓库已从“后端为主”推进到“前后端闭环可跑”，并满足“无 Docker 本地运行”的开发路径。
