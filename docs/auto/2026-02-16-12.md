# 自动/手工推进记录 - 2026-02-16 (12)

## 本次目标
- 按“后续从服务器启动”的要求，补齐非 Docker 服务器启动链路（可准备、可启动、可停机、可检查）。

## 变更摘要
- 根脚本新增（`package.json`）
  - `start:backend` / `start:admin` / `start:agent` / `start:all`
  - `build:all`
  - `server:prepare` / `server:start` / `server:stop` / `server:restart` / `server:status` / `server:health`

- 启动脚本新增（`scripts/`）
  - `server-prepare.sh`：`npm ci` + migrate + seed + build
  - `server-start.sh`：后台拉起三端，记录 pid/log
  - `server-stop.sh`：按 pid 优雅停止并兜底强杀
  - `server-status.sh`：进程状态 + HTTP 可用性
  - `server-health.sh`：调用 `smoke-live` 做联调健康检查

- 前端生产启动补齐
  - `admin-web/package.json`：`next start -H 0.0.0.0 -p 3001`
  - `agent-web/package.json`：`next start -H 0.0.0.0 -p 3002`

- 配置模板与文档
  - 新增 `admin-web/.env.production.example`
  - 新增 `agent-web/.env.production.example`
  - 新增 runbook：`docs/runbooks/server-startup.md`
  - README 补充“服务器启动（无 Docker）”章节 + `systemd` 开机自启示例
  - runbook 补充 `systemd` 开机自启配置与管理命令

- 运行目录忽略规则
  - `.gitignore` 增加：`logs/`、`.run/`、前端 `.env.production`

## 结论
- 已形成可直接用于服务器常驻的启动方案（使用 `start` 而非 `dev/watch`），并具备状态观测与健康检查入口。
