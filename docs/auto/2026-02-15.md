# 自动/手工推进记录 - 2026-02-15

## 本次目标
- 继续补齐 Admin 核心 CRUD（团队/代理/网卡/政策），为后续结算写库做准备。
- 保持“无 Docker、本地可跑”，并用接口测试固化行为。

## 变更摘要
- backend:
  - 抽出可注入依赖的 `buildApp()`（便于 PGlite 接口测试）
  - Admin:
    - 团队：`/admin/teams`（增改查 + 成员增删/转移 + 成员列表含姓名/星级）
    - 代理：`/admin/agents`（创建 AGENT 登录 + profile；支持设置/清空上级；防环）
    - 政策：`/admin/policies`（增改查）
    - 网卡：`/admin/cards`（录入卡 + 归属转移 + 状态事件维护；列表展示当前归属与最新状态）
  - 新增测试工具 `setupAdminTestApp()`：每个测试文件独立 PGlite + 迁移 + admin 登录
  - 新增/完善接口测试覆盖上述路由（Fastify inject）
- docs:
  - `docs/PROGRESS.md`：勾选 Admin CRUD 相关项
  - `README.md`：补充已实现接口清单

## 运行的命令与结果
- `npm run typecheck`：成功
- `npm test`：成功

## 下一步
1. 实现 AGENT RBAC scope（self + 一级/二级）与代理端接口（同事/网卡脱敏）。
2. 开始结算写库：生成 DRAFT、按代理重算、幂等与锁、POSTED 后调整单。

