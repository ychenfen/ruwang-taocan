# 2026-03-07 Run Report

## What changed
- Re-tested SSH access to `114.67.88.220` on ports `22`, `22222`, and `42572`.
- Confirmed TCP reachability on all three ports.
- Confirmed SSH handshake on `22` is now normal and reaches authentication stage.
- Verified `/Users/yuchenxu/Downloads/bot.pem` is a valid RSA private key and derived its public key fingerprint.

## Commands
- `nc -vz -w 5 114.67.88.220 22`
- `nc -vz -w 5 114.67.88.220 22222`
- `nc -vz -w 5 114.67.88.220 42572`
- `ssh -i /Users/yuchenxu/Downloads/bot.pem -o ConnectTimeout=8 -o StrictHostKeyChecking=no root@114.67.88.220 'date && hostname && whoami'`
- `ssh-keygen -lf /Users/yuchenxu/Downloads/bot.pem`
- `ssh-keygen -y -f /Users/yuchenxu/Downloads/bot.pem | ssh-keygen -lf -`

## Results
- Port `22` now returns normal OpenSSH handshake and reaches auth.
- `bot.pem` fingerprint is `SHA256:G4FldNv1tpTdD2vzajCMakdHH5ssr2mdw8AM6Ui8S+M`.
- Server rejects both this key and the previously supplied root password over SSH.
- Current blocker is no longer network or banner timeout; it is SSH authentication mismatch.

## Next step
- Install the public key derived from `bot.pem` into `/root/.ssh/authorized_keys` on the server console, or provide the correct SSH user/password pair.
- After auth works, proceed with the user's requested server cleanup.

## Blockers
- No working SSH credential yet for `114.67.88.220`.
