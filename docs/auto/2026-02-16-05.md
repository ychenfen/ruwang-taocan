# 自动/手工推进记录 - 2026-02-16 (05)

## 本次目标
- 继续完善导出能力，补齐 Ledger 分录的 Excel 导出与前端可用入口。

## 变更摘要
- 后端：Ledger 新增 XLSX 导出
  - 文件：`backend/src/routes/admin/ledger.ts`
  - 新增接口：`GET /admin/ledger/entries.xlsx`
  - 与 CSV 共用相同筛选口径与字段列顺序：
    - `commissionMonth/sourceType/settlementRunId/beneficiaryAgentId`
  - 对齐字段：
    - `commissionMonth, sourceType, entryId, sourceId, settlementRunId, entryNote, entryCreatedAt, lineId, settlementItemId, cardId, cardNo, beneficiaryAgentId, beneficiaryName, kind, targetKind, periodType, amount, lineCreatedAt`

- 后端测试与权限回归
  - 文件：`backend/src/routes/admin/ledger.test.ts`
    - 新增 `/admin/ledger/entries.xlsx` 返回断言（content-type + 非空内容）。
  - 文件：`backend/src/routes/rbac.test.ts`
    - 新增 AGENT 禁止访问 `/admin/ledger/entries.xlsx` 断言。

- 前端：Admin 分录页导出增强
  - 文件：`admin-web/app/(app)/ledger/page.tsx`
    - 导出从“仅 CSV”升级为“CSV + XLSX”双按钮。
    - 统一下载逻辑为 `exportLedger(format)`，按格式命名文件。

- 文档同步
  - 更新：`README.md`（增加 Ledger CSV/XLSX 导出接口说明）
  - 更新：`docs/PROGRESS.md`

## 验证结果
- `npm -w backend test` 通过
- `npm -w backend run typecheck` 通过
- `npm -w admin-web run typecheck` 通过
- `npm -w admin-web run build` 通过
- `npm -w agent-web run build` 通过

## 结论
- Ledger 导出能力已与报表导出能力对齐：支持 CSV 与 XLSX 双格式，并且管理端页面可直接使用。
