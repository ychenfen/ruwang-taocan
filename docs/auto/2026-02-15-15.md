# 自动/手工推进记录 - 2026-02-15 (15)

## 本次目标
- 继续完善后台产品化体验：总览页从多接口全量拉取改为单接口聚合统计。

## 变更摘要
- 新增后台统计接口
  - `GET /admin/stats`
  - 返回：
    - `agentsTotal`
    - `cardsTotal`
    - `teamsTotal`
    - `cardsOnNet`
    - `latestRun`（最新结算单）
    - `latestExecution`（最近跑批执行记录）
  - 文件：`backend/src/routes/admin/stats.ts`

- 路由注册
  - 在 admin 路由入口注册 `adminStatsRoutes`
  - 文件：`backend/src/routes/admin/index.ts`

- 回归测试
  - 新增 `backend/src/routes/admin/stats.test.ts`
  - 覆盖：
    - 未登录访问 401
    - 聚合字段与最新运行信息返回正确

- 管理端总览页改造
  - 从并行拉取 `/admin/agents` `/admin/cards` `/admin/teams` 改为主用 `/admin/stats`
  - 保留 `latestRun` 兜底读取
  - 增加“最近跑批”展示（月份/状态/触发源/耗时）
  - 文件：`admin-web/app/(app)/dashboard/page.tsx`

## 验证结果
- `npm -w backend test`：成功（13 文件、21 用例通过）
- `npm run typecheck`：成功
- `npm -w admin-web run build`：成功

## 结论
- 管理后台首页请求成本更低，统计信息与跑批状态展示更集中，可维护性更高。
