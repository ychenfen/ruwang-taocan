# 自动/手工推进记录 - 2026-02-16 (08)

## 本次目标
- 继续完善可运营能力：补齐审计导出接口与统计看板，并统一前端导出链路。

## 变更摘要
- 审计导出能力补齐
  - 文件：`backend/src/routes/admin/audit-logs.ts`
    - 新增：
      - `GET /admin/audit-logs.csv`
      - `GET /admin/audit-logs.xlsx`
      - `GET /admin/audit-logs/export-summary`
    - 支持：按 `entityType/entityId/action/actorUserId` 导出筛选；统计最近 N 天导出行为（总量、CSV/XLSX、分 action/actor、按日分布）。
  - 文件：`backend/src/routes/admin/audit-logs.test.ts`
    - 新增回归：列表筛选 + CSV/XLSX 导出 + export summary 聚合断言。
  - 文件：`backend/src/routes/rbac.test.ts`
    - 新增 RBAC 断言：AGENT 访问 `audit-logs.csv` / `audit-logs.xlsx` / `audit-logs/export-summary` 被拒绝。

- Admin 导出链路统一（移除页面裸 `fetch`）
  - 文件：`admin-web/lib/api.ts`
    - 新增 `apiDownload(path, init)`：统一 Bearer 鉴权、错误规范化、文件名解析（`content-disposition`）。
  - 文件：
    - `admin-web/app/(app)/reports/page.tsx`
    - `admin-web/app/(app)/ledger/page.tsx`
    - `admin-web/app/(app)/settlements/[runId]/page.tsx`
    - `admin-web/app/(app)/audit-logs/page.tsx`
    - 导出逻辑改为调用 `apiDownload`，避免页面直连 `fetch`。

- 审计页体验增强
  - 文件：`admin-web/app/(app)/audit-logs/page.tsx`
    - 导出行为统计新增“按日明细”表（`byDay`）。
    - 对 summary 的 `action` 参数做白名单兜底（仅导出动作可参与该筛选），避免普通 action 导致 400。

- 文档同步
  - 文件：`README.md`
    - 补充审计接口清单（`/admin/audit-logs.csv`、`/admin/audit-logs.xlsx`、`/admin/audit-logs/export-summary`）。
  - 文件：`docs/PROGRESS.md`
    - 新增本轮“审计导出增强/审计统计增强”勾选项。

## 验证结果
- `npm -w backend test` 通过
- `npm run verify` 通过（含 test + typecheck + admin build + agent build）

## 结论
- 审计中心从“可查询”升级为“可导出 + 可统计 + 可追踪”闭环。
- 前端导出链路实现统一封装，后续新增导出页面可直接复用 `apiDownload`，降低鉴权与错误处理重复风险。
