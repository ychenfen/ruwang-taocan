# 自动/手工推进记录 - 2026-02-16 (13)

## 本次目标
- 修复结算页“NOT_DRAFT 重算失败”可用性问题。
- 管理端核心页面中文化（结算、结算详情、入账分录、网卡状态）。
- 补齐删除能力（删除草稿账单、删除网卡、成员删除文案明确化）。

## 变更摘要
- 后端接口增强
  - 新增 `DELETE /admin/settlements/runs/:id`
    - 仅 `DRAFT` 可删，非草稿返回 `RUN_NOT_DRAFT`
    - 清理关联 `settlement_execution_logs`
    - 若有关联分录则拦截 `RUN_HAS_LEDGER_ENTRIES`
  - 新增 `DELETE /admin/cards/:id`
    - 仅未参与结算行项目网卡可删
    - 若已参与结算返回 `CARD_HAS_SETTLEMENT_ITEMS`

- 前端交互与中文化
  - `admin-web/app/(app)/settlements/page.tsx`
    - 处理 `NOT_DRAFT`：自动提示“已入账/已审核不可重算”并给出跳转入口
    - 状态/执行记录中文化
    - 新增草稿账单删除按钮
  - `admin-web/app/(app)/settlements/[runId]/page.tsx`
    - 状态、类型、期别、字段名中文化
    - 新增“删除草稿账单”按钮（仅 DRAFT）
  - `admin-web/app/(app)/ledger/page.tsx`
    - 分录来源/列名/类型/期别中文化
  - `admin-web/app/(app)/cards/page.tsx`
    - 网卡状态中文化
    - 列表支持删除网卡
  - `admin-web/app/(app)/cards/[cardId]/page.tsx`
    - 网卡状态中文化
    - 详情页支持删除网卡
  - `admin-web/app/(app)/teams/[teamId]/page.tsx`
    - “移除”调整为“删除成员”
  - `admin-web/app/(app)/teams/page.tsx`
    - 团队标签/状态等字段中文化

- 路由注册
  - `backend/src/routes/admin/index.ts` 注册 `adminSettlementDeleteRoutes`

- 测试新增/更新
  - `backend/src/routes/admin/cards.test.ts`
    - 新增删除网卡成功/拦截用例
  - 新增 `backend/src/routes/admin/settlement-delete.test.ts`
    - 覆盖删除 DRAFT 成功与非 DRAFT 拦截

## 验证结果
- `fnm exec --using v22.17.0 -- npm -w backend run typecheck`：通过
- `fnm exec --using v22.17.0 -- npm -w backend run test -- src/routes/admin/cards.test.ts`：通过（2 tests）
- `fnm exec --using v22.17.0 -- npm -w backend run test -- src/routes/admin/settlement-delete.test.ts`：通过（2 tests）
- `fnm exec --using v22.17.0 -- npm -w admin-web run build`：通过

