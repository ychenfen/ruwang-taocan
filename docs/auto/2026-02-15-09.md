# 自动/手工推进记录 - 2026-02-15 (09)

## 本次目标
- 推进 M9（运维与部署）：Docker 化、生产 compose、备份恢复 runbook、基础 CI。

## 变更摘要
- 后端容器化
  - 新增：`backend/Dockerfile`
  - 启动流程：容器启动后先 `npm run migrate`，再 `npm run start`
  - 新增后端启动脚本：`backend/package.json` -> `"start": "tsx src/server.ts"`

- 生产编排
  - 新增：`infra/docker-compose.prod.yml`
  - 包含服务：
    - `db`（Postgres 16 + healthcheck + 数据卷）
    - `backend`（依赖 db healthy，读取 `JWT_SECRET` 与 `DATABASE_URL`）

- 备份恢复
  - 新增：`docs/runbooks/backup-restore.md`
  - 覆盖：
    - `pg_dump -Fc` 逻辑备份
    - `pg_restore` 新库恢复流程
    - 恢复后接口与数据校验清单

- CI
  - 新增：`.github/workflows/ci.yml`
  - 流程：`npm ci` -> `npm run typecheck` -> `npm test`

- 其他
  - 新增：`.dockerignore`（减小构建上下文）
  - 更新：`README.md` 增加生产部署说明（Docker + runbook 链接）

## 验证结果
- `npm run typecheck`：成功
- `npm test`：成功

## 结论
- M9 三项核心交付已落地：部署、备份恢复、CI。
