{
  "name": "@ruwang/backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "migrate": "tsx scripts/migrate.ts",
    "seed": "tsx scripts/seed.ts",
    "perf:baseline": "tsx scripts/slow-query-baseline.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "NODE_OPTIONS=--no-experimental-require-module vitest run",
    "test:watch": "NODE_OPTIONS=--no-experimental-require-module vitest"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.3.15",
    "@fastify/cors": "^8.5.0",
    "@fastify/jwt": "^8.0.0",
    "dotenv": "^16.4.5",
    "fastify": "^4.26.2",
    "pg": "^8.11.5",
    "xlsx": "^0.18.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.15.5",
    "tsx": "^4.16.2",
    "typescript": "^5.4.5",
    "vitest": "^2.1.9"
  }
}
