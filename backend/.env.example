PORT=3000
JWT_SECRET=change-me-change-me
TZ=Asia/Shanghai
# If DATABASE_URL is unset, backend will use embedded Postgres (PGlite) at PGLITE_PATH.
# DATABASE_URL=postgres://user:pass@localhost:5432/dbname
PGLITE_PATH=./.data/pglite
